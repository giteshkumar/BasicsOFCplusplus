#inlcude<stdio>

int main(int argc , char **argv)

{

	char ageString[10];
	int age;
	int bonus;
printf("Enter the Age:");
gets(agestring);
age = atoi(agestring);
if (age == 0)
{
printf("you entered invalid age , so your bonus cannot be );
}
else{ 
if (age == 0)
bonus = 1000;
}
else
{
bonus = 500;
}
printf("your age is %d, so your bounus is %d.\n", age, bonus);
}

